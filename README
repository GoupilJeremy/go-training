CardsProject for training

with Travis CI : branch master : <img src="https://travis-ci.com/GoupilJeremy/go-training.svg?branch=master" alt="build:passed">
add type
add slices
add struct
play with pointers and reference
add interfaces

